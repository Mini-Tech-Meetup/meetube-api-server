spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:20000/meetube
    username: meetube
    password: postgresql
  flyway:
    enabled: true
    baseline-on-migrate: true
  profiles:
    active: test
